# Dependencies
node_modules/

# Uploaded attachment files (metadata lives in the DB; bytes are local/disk)
backend/uploads/
uploads/

# Secrets & active local config — never commit (keep the .example templates instead)
.env
frontend/env/config.js

# Claude Code local settings
.claude/

# Logs, temp, OS junk
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Local demo-prep notes (not part of the application)
CCMS-DEMO-GAMEPLAN.txt
CCMS-Project-Explanation.html
